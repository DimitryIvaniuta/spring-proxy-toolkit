# spring-proxy-toolkit
Spring Boot / Java "custom proxy toolkit" using ProxyFactory + BeanPostProcessor to chain Audit + Idempotency + Cache + Rate limiting + Retry advices, with PostgreSQL + Flyway + JPA persistence
